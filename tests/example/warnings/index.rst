sphinx-mounts warnings
======================

Every warning this extension can emit has a commented-out ``[[mounts]]``
block in ``ubproject.toml``. Uncomment one block at a time and rebuild to
see the warning it triggers — each block carries a comment explaining why.

.. toctree::

   more
